# Required dependencies for the suite-owned PPTX runtime and QA.
# LibreOffice and Poppler are system tools and must be installed separately.
jsonschema>=4.0
PyYAML>=6.0
Pillow>=10.0
markitdown[pptx]>=0.1.0,<1.0
defusedxml>=0.7.1,<1.0
